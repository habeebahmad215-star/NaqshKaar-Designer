name: Build NaqshKaar Designer

on:
  workflow_dispatch:
  push:
    branches:
      - main

jobs:
  build:
    name: Build Android
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v6

      - name: Setup Flutter
        uses: subosito/flutter-action@v2
        with:
          flutter-version: 3.47.0
          channel: stable
          cache: true

      - name: Check Flutter
        run: flutter --version

      - name: Generate Android project
        run: flutter create --platforms=android --org com.naqshkaar .

      - name: Install Android API 36
        run: |
          yes | "$ANDROID_HOME/cmdline-tools/latest/bin/sdkmanager" --licenses >/dev/null 2>&1 || true
          "$ANDROID_HOME/cmdline-tools/latest/bin/sdkmanager" "platforms;android-36"

      - name: Configure Android plugin compatibility
        run: |
          printf '\nandroid.builtInKotlin=false\n' >> android/gradle.properties
          printf 'android.newDsl=false\n' >> android/gradle.properties

      - name: Set Android compile SDK to 36
        run: |
          sed -i -E 's/^[[:space:]]*compileSdk[[:space:]]*=.*/    compileSdk = 36/' android/app/build.gradle.kts
          grep -n "compileSdk" android/app/build.gradle.kts

      - name: Download bundled Urdu fonts
        run: |
          mkdir -p assets/fonts
          curl -fL --retry 3 -o assets/fonts/Gulzar-Regular.ttf \
            https://raw.githubusercontent.com/googlefonts/Gulzar/main/fonts/ttf/Gulzar-Regular.ttf
          curl -fL --retry 3 -o assets/fonts/NotoNastaliqUrdu-Regular.ttf \
            https://raw.githubusercontent.com/harfbuzz/harfbuzz-monster-fonts/main/NotoNastaliqUrdu-Regular.ttf
          curl -fL --retry 3 -o assets/fonts/MehrNastaliq.ttf \
            https://raw.githubusercontent.com/abbassiddiqi/mehr/master/mehr.ttf
          curl -fL --retry 3 -o assets/fonts/AlviNastaleeq.ttf \
            https://raw.githubusercontent.com/imrofayel/UrduFonts/master/fonts/AlviNastaleeq.ttf
          curl -fL --retry 3 -o assets/fonts/JameelNooriNastaleeq.ttf \
            https://raw.githubusercontent.com/abid-mujtaba/ttf-jameel-noori-nastaleeq/master/Jameel%20Noori%20Nastaleeq.ttf
          curl -fL --retry 3 -o assets/fonts/AlMajeedQuranic.ttf \
            https://fontly-virid.vercel.app/fonts/ttf/al-majeed-quranic-font-shiped.ttf
          curl -fL --retry 3 -o assets/fonts/BombayBlack.ttf \
            https://www.urdufont.org/download.php?id=737645
          for font in assets/fonts/*.ttf; do test -s "$font" || exit 1; done

      - name: Get dependencies
        run: flutter pub get

      - name: Remove generated default test
        run: rm -rf test

      - name: Normalize generated compatibility issue
        run: sed -i 's/Icons\.rotate_0_degrees_ccw_rounded/Icons.rotate_left_rounded/g' lib/screens/workspace_screen.dart

      - name: Apply premium Urdu font catalog
        run: python3 scripts/font_catalog_upgrade.py

      - name: Apply final professional toolbar polish
        run: python3 scripts/final_toolbar_polish.py

      - name: Analyze
        run: flutter analyze

      - name: Build APK
        run: flutter build apk --release

      - name: Build Play Store AAB
        run: flutter build appbundle --release

      - name: Upload APK
        uses: actions/upload-artifact@v4
        with:
          name: NaqshKaar-Designer-APK
          path: build/app/outputs/flutter-apk/app-release.apk

      - name: Upload Play Store AAB
        uses: actions/upload-artifact@v4
        with:
          name: NaqshKaar-Designer-AAB
          path: build/app/outputs/bundle/release/app-release.aab
